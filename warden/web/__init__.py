"""Warden web dashboard — the live operator console (required web surface).

Built on the Python standard library (no framework needed to run locally), it
streams Warden's reasoning over Server-Sent Events, renders the fleet and the
incident ledger, and exposes a real human-in-the-loop Approve/Deny control.
"""
